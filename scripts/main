#!/usr/bin/env bash

set -o pipefail

source ./scripts/utils.sh
source ./scripts/install-aws-cli.sh
source ./install-nvm

echo $PWD

# INSTALL APPS
echo_info "Installing applications (${cyan}Google Chrome, VScode${normal}) via Homebrew"
# brew bundle --file "$DOTFILES_PATH/Brewfile"